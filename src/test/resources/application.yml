spring:
    jpa:
        show-sql: true
        properties:
            hibernate:
                format-sql: true
    datasource:
        driver-class-name: org.h2.Driver
        password: sa
        url: jdbc:h2:mem:db;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
        username: sa
